# Public environment variables for the production environment.
#
# Only non-sensitive variables belong here. All keys are validated against
# deployment/schema.yml — sensitive variables (private keys, auth tokens)
# will be rejected by the pre-commit hook and CI.
#
# Sensitive variables are managed directly in Vercel and are never committed.
# Run `pnpm env:pull` to generate a local .env.local from Vercel's stored values.
#
# To update these values and sync them to Vercel:
#   scripts/update-config.sh --env=production --firebase-config=/path/to/config.json --sync
# Or to sync the current values without changes:
#   scripts/deploy-config.sh --env=production

environment: production

variables:
  # Firebase Client SDK — safe to expose; access control is enforced by Firebase security rules
  NEXT_PUBLIC_FIREBASE_API_KEY: "AIzaSyDHRbEXEn0SkdY7gIDrvJzVM-_ufy9iMnA"
  NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: "hidden-role-game-e530f.firebaseapp.com"
  NEXT_PUBLIC_FIREBASE_PROJECT_ID: "hidden-role-game-e530f"
  NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: "hidden-role-game-e530f.firebasestorage.app"
  NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: "443493109740"
  NEXT_PUBLIC_FIREBASE_APP_ID: "1:443493109740:web:f6e2640ae5b486d6d13ac9"
  NEXT_PUBLIC_FIREBASE_DATABASE_URL: "https://hidden-role-game-e530f-default-rtdb.firebaseio.com"

  # Firebase Admin SDK — project identifier only, not a credential
  FIREBASE_PROJECT_ID: "hidden-role-game-e530f"
  FIREBASE_DATABASE_URL: "https://hidden-role-game-e530f-default-rtdb.firebaseio.com"
