# Avalon — Actions

Avalon has no app-managed actions. The game (`actions: {}`) relies entirely on out-of-band player discussion and physical voting; the app serves only as a role-distribution and team-visibility tool.
