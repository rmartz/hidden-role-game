name: Secret Scan

on:
  pull_request:
  push:
    branches: [main]

jobs:
  secret-scan:
    uses: rmartz/vercel-deploy-scripts/.github/workflows/secret-scan.yml@v3.1.0
    timeout-minutes: 2
    with:
      config-path: .gitleaks.toml
    secrets: inherit
